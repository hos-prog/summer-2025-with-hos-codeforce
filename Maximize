from math import gcd
t=int(input())
for _ in range(t):
    n=int(input())
    GCD_dict={}
    for i in range(1,n):
        m=gcd(n,i)
        v=m+i
        GCD_dict[i]=v
    maximum=max(GCD_dict,key=GCD_dict.get)
    #this syntax i search from google
    print(maximum)

