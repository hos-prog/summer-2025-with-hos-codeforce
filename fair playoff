#hos
t=int(input())
for _ in range(t):
    s1,s2,s3,s4=map(int,input().split())
    max1=max(s1,s2)
    max2=max(s3,s4)
    min1=min(s1,s2)
    min2=min(s3,s4)
    if max1<min2 or max2<min1:
        print("NO")
    else:
        print("YES")    